.libPaths(c('/usr/local/R-3.0.2/lib/R/library/', .libPaths()))

options(BioC_mirror="http://bioconductor.statistik.tu-dortmund.de")
rep_path <- file.path(R.home("etc"), "repositories")
repos <- tools:::.read_repositories(rep_path)

ind <- match(c("BioCsoft", "BioCann", "BioCexp","BioCextra" ), rownames(repos) )
newrepos <- c(repos[ind, "URL"], c(CRAN= "http://stat.ethz.ch/CRAN"))
options(repos=newrepos)


